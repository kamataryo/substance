const walker = (obj) => {
  const keys =
}

export const substance = (arg: object | string) => {
  const json = typeof arg === "string" ? JSON.parse(arg) : arg;
  const result = {}
  for
};

export default substance;
